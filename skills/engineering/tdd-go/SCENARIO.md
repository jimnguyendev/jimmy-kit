# Scenario — tdd-go (written BEFORE the routing-name revision)

**Rationale:** a green behavior slice can expose a wider architecture problem; the implementation owner must preserve scope and hand evidence to the real bundled discovery skill.

**Sample input:** "The accepted pagination fix is green. It revealed duplicated repository ownership across three other features outside this slice."

**Expected behaviors:**
- [ ] Keeps the accepted behavior slice bounded and green.
- [ ] Routes wider discovery to `improve-codebase-architecture`.
- [ ] Carries evidence, owned scope, verification state, and return condition.
- [ ] Does not treat the broader candidate as already approved cleanup.

**Status:** [EXIT 2 — scenario specified; no independent fresh-agent run recorded yet].

## Package-change extension (written BEFORE the philosophy revision)

When an accepted Go behavior creates or moves packages, types, or interfaces, implementation must keep the import graph acyclic, preserve business-capability locality, avoid name stuttering, keep types near their owner, and introduce a consumer-side interface only for a real seam.
