# Scenario — ux-writing (written BEFORE the path correction)

**Rationale:** a durable voice guide should be discoverable without writing into the target repository's own docs tree.

**Sample input:** "Create a voice and tone guide because this project has none."

**Expected behaviors:**
- [ ] Reads an existing project-defined guide when supplied.
- [ ] Writes a new durable guide under `.jimmy/docs/`.
- [ ] Uses that guide for later microcopy decisions.

**Status:** [EXIT 2 — scenario specified; no independent fresh-agent run recorded yet].
