# Scenario — advanced-rfm-segmentation (added for the de-branding audit)

**Rationale:** the runnable Jenks example must remain useful without a source-company label.

**Sample input:** "Run the sample revenue classification."

**Expected behaviors:**
- [ ] The example executes with its existing sample data.
- [ ] Output uses generic practice-lab naming.
- [ ] Breaks, GVF, and tier calculations are unchanged.

**Status:** [EXIT 2 — scenario specified; no independent fresh-agent run recorded yet].
