# Scenario — growth-markov-duolingo (added for the de-branding audit)

**Rationale:** the runnable forecast example must not leak a source-company name.

**Sample input:** "Run the sample 14-day growth forecast."

**Expected behaviors:**
- [ ] The example executes with its existing sample matrix.
- [ ] Output uses generic practice-lab naming.
- [ ] Forecast calculations and labels are otherwise unchanged.

**Status:** [EXIT 2 — scenario specified; no independent fresh-agent run recorded yet].
