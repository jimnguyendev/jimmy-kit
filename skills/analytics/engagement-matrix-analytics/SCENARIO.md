# Scenario — engagement-matrix-analytics (written BEFORE the example correction)

**Rationale:** the runnable engagement example must remain reusable and brand-neutral.

**Sample input:** "Run the sample engagement-matrix calculation."

**Expected behaviors:**
- [ ] The example executes with its existing feature data.
- [ ] Comments use generic practice-lab naming.
- [ ] Breadth, frequency, and quadrant calculations are unchanged.

**Status:** [EXIT 2 — scenario specified; no independent fresh-agent run recorded yet].
